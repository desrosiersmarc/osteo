<div class="container">
  <div class="row">
    <h2>Liste des rdv | <%= link_to "back", root_path %> </h2>
    <% @appointments.each do |appointment| %>
      <div class="col-xs-12 col-sm-6">
        <%= appointment.id %>
      </div>
    <% end %>
  </div>
</div>
